需要考虑:
- 使用 opfs 存储
- 数据迁移: 1. in browser 数据迁移(优先). 2: exported 数据(file)迁移脚本 (优先级低 hold).
- 数据同步方案设计
- 搜索方案设计: 建立本地索引 (indexedDB) + 搜索
- 数据解析设计: 保证当前功能/ui/ux 完全不变, 切换到重构的数据
- 导入导出: 采用与底层存储完全一致的目录结构。通过 zip 导入导出

hold (未来需要，0.2.0 不需要)
- 携带图片和附件
- 数据库表格 (mini notion)
- 图片笔记（展示为图片/相册等形式）
- \`\[\[xxx\]\]\` 双链 (兼容 obsidian)
- 嵌入/引用/块引用 (兼容 obsidian)

功能细节
- 保存和同步：手动保存，自动同步

要求:
- 注意统一时区: Z

目标和计划:
- 产出设计方案
- 支持 web 端
- 支持 extension 端